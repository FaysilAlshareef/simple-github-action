name: First Workflow
on:
  push:
    branches:
      - main
jobs:
  example-job:
    runs-on: ububntu-latest
    steps:
      - name: Print a walcome message
        run: echo "Welcome to github actions"
